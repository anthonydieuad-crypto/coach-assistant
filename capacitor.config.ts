import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lnrv.coachassistant',
  appName: 'coach-assistant',
  webDir: 'dist'
};

export default config;
